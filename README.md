# SQL Lab Assignment – Course Table

## Objective

To create a table, insert records, and display the table structure using SQL commands.

## Problem Statement

Create a table named `Course` with the following fields:

| Field Name |
|---|
| CourseID |
| CourseName |
| Credits |
| DepartmentID |

Perform the following operations:

1. Create the `Course` table.
2. Add an appropriate PRIMARY KEY constraint to `CourseID`.
3. Insert at least 3 records.
4. Display the table structure using the `DESCRIBE` command.
5. Display all records from the `Course` table.

## Instructions

1. Open `student_solution.sql`.
2. Write your SQL commands in that file.
3. Do not rename `student_solution.sql`.
4. Do not modify `test.sh`.
5. Do not modify `.github/workflows/autograding.yml`.
6. Insert at least 3 records.
7. Commit and push your changes.
8. Check the **Actions** tab for your marks.

## Marks Distribution

| Test Case | Marks |
|---|---:|
| Course table created | 2 |
| CourseID column | 1 |
| CourseName column | 1 |
| Credits column | 1 |
| DepartmentID column | 1 |
| At least 3 records inserted | 2 |
| CourseID Primary Key | 1 |
| Table structure correct | 1 |
| **Total** | **10** |

## Submission

Complete only:

```text
student_solution.sql
```

Do not modify the test or workflow files.
